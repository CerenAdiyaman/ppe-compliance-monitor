# Runtime dependencies. Install the package itself with `pip install -e .`
ultralytics>=8.2
opencv-python>=4.9
numpy>=1.26
pydantic>=2.7
pydantic-settings>=2.2
SQLAlchemy>=2.0
python-dotenv>=1.0

# Optional API layer (phase 2)
fastapi>=0.111
uvicorn[standard]>=0.29
